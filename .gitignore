# Betreiber-/Operator-Schluessel niemals versionieren oder deployen
OPERATOR-KEY.local.txt
*.local.txt

# Lokaler Deploy-Lauf: Log mit Server-Dialog (Passwort maskiert), nie versionieren
deploy-local.log

# Betriebssystem / Editor
.DS_Store
Thumbs.db
*.swp
*~

# Supabase-CLI (lokale Temp-/Zugangsdaten), nur Functions/Migrations bleiben
supabase/.branches/
supabase/.temp/
.env
.env.*
